import Foundation

/// Visible on-device so we know the phone is running THIS code, not an old install.
enum BuildStamp {
    static let id = "Build 0810-E"
    static let label = "EnviroMap · Build 0810-E"
}
